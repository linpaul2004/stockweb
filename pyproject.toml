[project]
name = "stockweb"
version = "1.4.3"
description = "A real-time Taiwan stock dashboard with intraday price charts and order book."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
    "twstock>=1.5.1",
    "yfinance>=1.5.2",
]
